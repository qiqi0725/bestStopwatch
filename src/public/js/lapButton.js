import Button from 'Button';

class LapButton extends Button {
    constructor(name) {
        this.lapNum = 1;
        super(name);
    }

    // Write lap number, lap time
    buttonAction(lapTime) {

    }
}